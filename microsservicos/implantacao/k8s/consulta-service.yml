apiVersion: v1
kind: Service
metadata:
  name: ecm516-consulta-service
spec:
  type: NodePort
  selector:
    app: consulta
  ports:
    - name: consulta
      protocol: TCP
      port: 6000
      targetPort: 6000
